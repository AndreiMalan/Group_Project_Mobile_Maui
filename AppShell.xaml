<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Proiect_Mobile_Maui_Onetiu_Malan.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Proiect_Mobile_Maui_Onetiu_Malan"
    Shell.FlyoutBehavior="Disabled">

    <!--<ShellContent
        Title="Destination List"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />-->
    <TabBar>
        <ShellContent
            Title="Destination List"
            ContentTemplate="{DataTemplate local:MainPage}"
           />
        <ShellContent
            Title="About"
            ContentTemplate="{DataTemplate local:AboutPage}"
        />
        <ShellContent
            Title="My Cities Lists"
            ContentTemplate="{DataTemplate local:ListEntryPage}"
        />
        <ShellContent
            Title="My Cities"
            ContentTemplate="{DataTemplate local:CityEntryPage}"
        />
    </TabBar>

</Shell>
